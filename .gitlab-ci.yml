image: ruby:2.1

pages:
  script:
  - gem install jekyll
  - gem install jekyll-sitemap
  - gem install jekyll-paginate
  - jekyll build -d public/
  artifacts:
    paths:
    - public
  only:
  - master
